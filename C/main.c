#include "PIP_winding.h"

int main(void)
{
    // 1. load polygon file
    // 2. Check the polygon poing valid
    // 3. confirm the position right now
    // 4. determine the target point inside or outside

    //for continueous calculating by realtime position updating.
    while(true)
    {
        //Not Available Now
        break;
    }
    return 0;
}